{% case media.media_type %}
    {% when 'image' %}
        <img src="{{media |  img_url: 'large'}}" alt="{{media.preview_image}}" class="w-full h-full object-cover">
    {% else %}
        
{% endcase %}